function mostrarMeGusta1() {
    const contador = document.getElementById("likes1");
    let texto = contador.textContent;
    let numero = parseInt(texto);
    numero++;
    contador.textContent = `${numero} like(s)`;
}

function mostrarMeGusta2() {
    const contador = document.getElementById("likes2");
    let texto = contador.textContent;
    let numero = parseInt(texto);
    numero++;
    contador.textContent = `${numero} like(s)`;
}

function mostrarMeGusta3() {
    const contador = document.getElementById("likes3");
    let texto = contador.textContent;
    let numero = parseInt(texto);
    numero++;
    contador.textContent = `${numero} like(s)`;
}
